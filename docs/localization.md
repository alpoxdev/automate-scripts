# Localization

Supported languages:

- English (`en`)
- Korean (`ko`)

Core strings live in `AutomateCore.Localizer`. The menu bar target also includes `Localizable.xcstrings` for SwiftUI resources.

Every user-facing addition should update both languages and include tests when practical.
